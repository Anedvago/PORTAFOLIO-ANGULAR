import { Routes } from '@angular/router';
import { InicioComponent } from './Modulos/Inicio/Pages/inicio/inicio.component';

export const routes: Routes = [
    { path: "", component: InicioComponent }
];
